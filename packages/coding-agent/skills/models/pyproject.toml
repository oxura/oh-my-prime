[project]
name = "oh-my-prime-model-mesh"
version = "0.1.0"
description = "Oh My Prime semantic model routing and verified capability matrix"
requires-python = ">=3.10"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/models"]
