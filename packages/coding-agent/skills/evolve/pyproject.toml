[project]
name = "oh-my-prime-evolution-lab"
version = "0.1.0"
description = "Proof-backed continual harness evolution for Oh My Prime"
requires-python = ">=3.10"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/evolve"]
