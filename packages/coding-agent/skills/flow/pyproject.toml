[project]
name = "oh-my-prime-flow"
version = "0.1.0"
description = "Durable typed task graph runtime for Oh My Prime"
requires-python = ">=3.10"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/flow"]
