[project]
name = "oh-my-prime-workspace"
version = "0.1.0"
description = "Oh My Prime isolated workspace manager with hash-gated promotion"
requires-python = ">=3.10"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/workspace"]
